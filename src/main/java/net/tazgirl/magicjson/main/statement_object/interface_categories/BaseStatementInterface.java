package net.tazgirl.magicjson.main.statement_object.interface_categories;

import net.tazgirl.magicjson.main.statement_object.StatementManager;

public interface BaseStatementInterface
{
    // Exists just so the interfaces can be passed around the same way as BaseStatementObject

    public void SpreadManager(StatementManager newManager);

}
